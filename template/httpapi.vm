package ${table.packageName}.http;

import org.apache.log4j.Logger;

import com.control.situation.utils.ResponseObj;
import com.control.situation.api.${table.className}Api;
import com.control.situation.config.Env;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import javax.servlet.http.HttpServletRequest;

/**
 * ${table.className} 访问层
 * @author ${author}
 * @since 1.0
 */
@RestController
@RequestMapping("/api/${table.classNameFirstLower}")
public class ${table.className}HttpApi {

	private Logger logger = Logger.getLogger(${table.className}HttpApi.class);

	@Autowired
	private ${table.className}Api ${table.classNameFirstLower}Api;

	@RequestMapping("findList")
	public ResponseObj findList(HttpServletRequest req) {
		Env env = (Env) req.getAttribute("env");
		ResponseObj ResponseObj = ${table.classNameFirstLower}Api.findList(env);
		return ResponseObj;
	}

	@RequestMapping("findDetail")
	public ResponseObj findDetail(HttpServletRequest req){
		Env env = (Env) req.getAttribute("env");
		ResponseObj ResponseObj = ${table.classNameFirstLower}Api.findDetail(env);
		return ResponseObj;
	}

	@RequestMapping("save")
	public ResponseObj save(HttpServletRequest req){
		Env env = (Env) req.getAttribute("env");
		ResponseObj ResponseObj = ${table.classNameFirstLower}Api.save(env);
		return ResponseObj;
	}

	@RequestMapping("delete")
	public ResponseObj delete(HttpServletRequest req){
		Env env = (Env) req.getAttribute("env");
		ResponseObj ResponseObj = ${table.classNameFirstLower}Api.delete(env);
		return ResponseObj;
	}

	@RequestMapping("update")
	public ResponseObj update(HttpServletRequest req){
		Env env = (Env) req.getAttribute("env");
		ResponseObj ResponseObj = ${table.classNameFirstLower}Api.update(env);
		return ResponseObj;
	}
}
